# Railway Environment Variables Configuration
# Copy this to set up your Railway deployment environment variables

# Core Settings
NODE_ENV=production
PORT=3001

# Storage Configuration
STORAGE_DIR=/app/server/storage

# Security Keys (Generate secure random strings)
JWT_SECRET=your-super-long-jwt-secret-here-at-least-32-characters
SIG_KEY=your-signature-key-here-at-least-32-characters
SIG_SALT=your-signature-salt-here-at-least-32-characters

# LLM Provider Configuration (Choose one)
LLM_PROVIDER=openai
OPENAI_API_KEY=your-openai-api-key-here

# Vector Database Configuration
VECTOR_DB=lancedb

# Audio Processing
WHISPER_PROVIDER=local
TTS_PROVIDER=native

# Privacy Settings
DISABLE_TELEMETRY=false

# Database (Railway will auto-configure if using Railway Postgres)
# DATABASE_URL will be automatically set by Railway if you add a Postgres service